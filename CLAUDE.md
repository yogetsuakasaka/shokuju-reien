# CLAUDE.md — 平成院サイト 運用マニュアル

このファイルは、**Claude Code がこのサイトを編集するときに必ず最初に読むルール**です。
人が読んでも分かるように日本語で書いてあります。スマートフォンから AI に編集を頼むときも、
このファイルの内容が前提になります。

---

## 1. このサイトの目的

**青松山 平成院 植樹葬霊園（岩手県九戸郡洋野町）の公式サイト**です。

サイトのゴールはただ一つ、**現地に来てもらうこと**です。

- 第一の目標：**霊園事務局への電話**（0194-77-4411）
- 第二の目標：見学のご相談

墓地は写真では決まりません。森の美しさは現地が証明します。
サイトの仕事は、**訪れる前の不安を一つずつ取り除くこと**です。

利用者の中心は 50〜80代のご本人と、その 40〜50代のご家族です。
**読みやすさは、美しさより優先します。**

### ブランドの中心

- タグライン：**「星と花と木のもとで。」**
- 中心の考え方：**「生きているうちから、自分の木と関係を結ぶ。」**
- 平成院の植樹葬は、**生前に自分で花木を選び、苗木を植え、没後はその木のもとへ埋蔵される**ものです。

---

## 2. 絶対に推測してはいけない情報 🚫

**このサイトで最も重要なルールです。**

確認できていないことを、それらしく書いてはいけません。
「たぶんこうだろう」「普通の霊園ならこうだ」で文章を埋めることを**固く禁止**します。

### 特に注意する項目

| 項目 | ルール |
|---|---|
| 料金 | `src/data/plans.json` に書かれた金額以外を書かない。**税込／税別と書かない**（未確認のため） |
| 50万円区画 | 規約 第15条は旧制度の規定。**現行案内は第24条・第25条の20万円／30万円**。料金表に50万円を追加しない（規約全文には原文のまま残し、注記を付けてある） |
| 区画の寸法 | 規約 第24条により「**専用地の中心から隣接地の中心まで**2.0メートル／1.5メートル」。**「直径」「一辺」と書かない** |
| 区画の人数 | 第2期植樹葬墓地は**未確認**。第15条第1項の「四体まで」は旧制度に係る規定で、準用範囲は解釈が必要。人数を書かない |
| 区画名・プラン名 | **正式資料の名称をそのまま使う**（樹木選択型墓地／樹木限定型墓地／永代供養墓）。言い換えたり、新しい分類を作らない |
| 花木の本数・種類数 | 「約50種類のヤマアジサイ」「約300本のシャクナゲ」は **2019年時点**の記載。現在の数量として書かない |
| 継承 | **「継承者不要」と断定しない**。事実として「墓地の使用は会員一代限り」「会費は没後、相続等により他人へ承継されない」「永代供養墓では平成院が祭祀承継者に代わって供養を継続する」と書く |
| 合同追悼式 | 「毎年必ず開催されます」と将来を保証しない。**「霊園規約では〜と定めています」**と書く |
| 土に還る時間 | 規約は「数年経てば土に溶ける埋蔵用容器」と定めている。**それ以上の断定（何年で、どうなる）をしない** |
| 所要時間・交通 | 八戸から◯分などの数字は**未確認**。実測するまで書かない |
| 駐車場・見学予約 | 未確認。「事務局へお問い合わせください」と書く |

### 未確認のことを書きたくなったら

1. **書かない**、または
2. 「霊園事務局へお問い合わせください」と書く、または
3. どうしても仮に置く必要があるときだけ `{{TODO:確認したい内容}}` と書く

`{{TODO:...}}` が公開ファイルに残っていると、**`npm run build` が失敗します**（`scripts/check-todo.mjs`）。
これは事故防止の仕組みなので、無効化しないでください。

### 他の霊園・一般的な樹木葬との比較を書かない 🚫

**根拠資料のない比較表現は、サイトに一切書きません。**

- 🚫 「一般的な樹木葬では〜」「多くの霊園では〜」「他の樹木葬とは違い〜」「通常は〜」
- 🚫 「◯◯なのは平成院だけ」「東北で唯一の〜」
- ✅ 「平成院では〜」「この植樹葬では〜」「この場所の特徴は〜」

平成院の良さは、**他と比べずに、自分たちの事実をそのまま書く**ことで伝えます。
競合比較を未来永劫禁止するわけではありませんが、掲載するには出典のある調査が必要です。
現段階では書かない方針です。

### ブランドコピーと制度上の事実を混ぜない

- ✅ ポエティックな一行（「一本の木を、植える。」）→ 物語セクションに置く
- ✅ 制度の説明（「植樹できる期間は原則4月20日〜10月31日です」）→ 事実として明記する
- 🚫 「毎日でも会いに行ける墓地」のように、**制度上そうであるかのように読める比喩**は書かない
- 🚫 「森の会員」など、**正式な制度名でない造語**を使わない（正式には「会員」）

---

## 3. 料金を更新する

編集するのは **`src/data/plans.json` の1ファイルだけ**です。HTML は触りません。

```
src/data/plans.json
├── plantingGraves[]   … 樹木選択型墓地 / 樹木限定型墓地
├── burialWork         … 第2期植樹葬墓地の埋葬作業代
├── eternal            … 永代供養墓（内訳・33年安置・合葬）
└── footnote           … 料金表の下に出る注記
```

金額を変えるときは、**数値の `price` と、表示用の `priceText` の両方**を直します。

```json
{ "price": 300000, "priceText": "300,000円" }
```

変更後は必ず `npm run build` を実行し、`/plans/` と TOP の両方に反映されたか確認します。

> AI への頼み方の例：
> 「plans.json の樹木選択型墓地を350,000円に変更して。priceText も直して。」

---

## 4. よくあるご質問を更新する

編集するのは **`src/data/faq.json` だけ**です。

```json
{
  "id": "parking",            // 英数字。URL の #faq-parking になる
  "q": "駐車場はありますか。",
  "a": ["1行目", "2行目"],     // 配列。1要素が1段落
  "top": false                // true にすると TOP ページにも抜粋表示される
}
```

- `/faq/` には全項目が出ます。
- TOP に出るのは `top: true` の項目だけです（現在5件）。増やしすぎないこと。
- **FAQPage 構造化データもこのファイルから自動生成**されます。回答の1文目で結論を言い切ってください。
- 回答は、旧公式サイトと霊園規約で確認できた内容だけで書きます。

---

## 5. 写真を追加する

写真はまだ1枚も入っていません。**iPhone から直接アップロードできる運用**にしてあります。

### 手順

1. **iPhone で位置情報を外す（必ず）**
   - 写真アプリで共有 →「オプション」→**「位置情報」をオフ**→「"ファイル"に保存」
   - **このリポジトリは公開されています。** EXIF に GPS 座標が含まれるため、外さずに
     コミットすると撮影場所が誰にでも分かる状態になります
   - 外し忘れても `npm run build` が検出して止めます（`scripts/check-exif.mjs`）。
     ただしコミット後だと履歴に残るので、**アップロード前に外すのが確実**です

2. **`src/images/work/` にアップロードする**
   - 長辺3000px程度（Hero候補のみ4000px）
   - ファイル名は半角英数字・小文字・ハイフン（例：`02-aerial-spring.jpg`）
   - 詳細は `src/images/work/README.md`

3. **`scripts/image-recipes.json` に切り出し方を書く**

   ```json
   { "key": "hero", "out": "hero/hero-sp.jpg", "source": "02-aerial-spring.jpg",
     "aspect": "9:16", "fx": 0.5, "fy": 0.42, "width": 1400 }
   ```

   - `fx` / `fy` … 残したい場所（0〜1。中央が 0.5, 0.5）
   - `zoom` … 1より大きいと寄る
   - `adjust` … `{ contrast, brightness }` のみ。**色味（彩度・色相）は変更しません**

4. **生成する**

   ```bash
   npm run images:dry     # 何が作られるか確認（書き込みなし）
   npm run images:apply   # 生成 + images.json の file を自動で紐付け
   npm run build
   ```

WebP への変換・複数サイズの生成・遅延読み込みは、そのあとビルドが自動で行います。

### フォルダの役割

| フォルダ | 役割 | Git | 公開ビルド |
|---|---|---|---|
| `src/images/work/` | **アップロード先。**切り出し前の写真 | 管理する | **出力されない** |
| `src/images/hero/` `story/` `seasons/` `monuments/` `grounds/` | 表示用。`npm run images` が生成 | 管理する | 出力される |
| `src/images/source/` | 元画像の保管庫（PC作業用・任意） | **管理外** | 出力されない |

### 大原則

- **アップロードした写真は読むだけ。** 上書き・削除はしません。派生画像は別ファイルです
- **出力から EXIF（GPS・機材情報）は自動的に除去されます**
- **色味は変更しません。** sRGB に変換してプロファイルを埋め込むだけです
  （iPhone の Display P3 写真からプロファイルを捨てると色がずれるため）
- 補正は**明るさとコントラストのみ**。強い HDR や彩度の変更はできない実装です
- `work/` と `source/` は `Figure.astro` / `Hero.astro` の glob から除外してあります。
  **この除外を外さないこと。** 外すと大きな元画像がそのまま公開されます

### 微調整のしかた

写真を見て直したくなったら、レシピの数値を1つ変えて `npm run images` を実行するだけです。

| やりたいこと | 変更 |
|---|---|
| 地平線をもう少し上に | `fy` を小さく（0.50 → 0.40） |
| もっと寄りたい | `zoom` を上げる（1 → 1.5） |
| 右端の建物を切りたい | `fx` を小さく（0.50 → 0.40） |
| 少し締めたい | `adjust: { "contrast": 1.05 }` |

### 手で入れることもできます

切り出し済みの写真を `src/images/` 直下のフォルダに置き、`images.json` の `file` に
書いても動きます。その場合も **EXIF は必ず外してください**（ビルドが止まります）。

- `file` が `null` のあいだは、**どんな写真が入る場所かを説明したプレースホルダー**が表示されます
- `alt` は必ず書きます。「桜」ではなく「満開の西行桜」のように、**情報として**書いてください
- `want` は撮影・選定のための覚え書きです。プレースホルダーに表示されます

### Hero の写真について

- `hero` … スマートフォン用。**9:16 の縦構図を推奨**
  （全画面 cover 表示のため、3:4 だと左右が約38%切られます）
- `hero-wide` … パソコン用。**3:2 を推奨**（16:9 だと左右が約20%切られます）。
  設定しなければパソコンでも `hero` が使われます
- どちらも**画面下40%に文字とグラデーションが重なります**。下側は空・森など静かな面に

### 写真の禁止事項 🚫

- **AI生成画像を使わない**
- **外部のストック写真を使わない**（平成院で実際に撮影したものだけ）
- **合同追悼式の写真は、参列者の顔が判別できない構図にする**（引き・後ろ姿・手元）。
  掲載同意が確認できていない写真を使わない

---

## 6. デザインルール

### 色

色は**写真に担当させます**。UI は紙・墨・石の3系統だけです。

```
--paper  #faf8f5   背景（温かいオフホワイト）
--stone  #f2eee7   セクション背景（淡い石／ベージュ）
--ink    #1b1a17   本文（墨）
--ink-2  #4a4741   補助テキスト
--ink-3  #6f6b63   注記（これより薄い色を本文に使わない）
```

- **桜色・緑をUIのアクセントに使わない。** ボタンも墨色です。
- 定義は `src/styles/global.css` の `:root` にまとまっています。**色を変えるときはここだけ**。

### 文字

- 本文：ゴシック（`system-ui` / ヒラギノ角ゴ / Noto Sans JP）**18px 基準・行間1.9**
- 見出しと詩的なコピー：明朝（`.serif` クラス、または各見出しのスタイル）
- **Webフォントは1つも読み込んでいません。** 端末にあるフォントだけを使います。
  読み込み時間を増やすので、**安易にWebフォントを追加しないこと**。

### 動き

許可されているのは次の3つだけです。

- Fade
- ひかえめな Reveal（`data-reveal` 属性を付けるだけ）
- Hero のごくゆっくりした拡大

**禁止：** スクロールジャック、Lenis 等のスクロール改変、Hero動画、プリローダー、自動再生の音、
派手なアニメーション。

`data-reveal` は、**JavaScript が動いたときだけ**初期状態を隠します。
JS が無効・失敗しても本文は最初から読めます。この仕組みを壊さないでください。

### レイアウト

- **スマートフォンが第一のキャンバス**です。まずスマホで確認してから、パソコンを見ます。
- 読み物の幅は `.wrap-text`（34rem）、写真は `.wrap`（72rem）
- Hero は `100svh`（`100vh` は iOS でアドレスバーによって崩れるため使わない）

---

## 7. アクセシビリティのルール

主な利用者は 50〜80代です。次は**必ず守ります**。

- 本文コントラストを十分に取る（`--ink-3` より薄い色を本文に使わない）
- **フォーカス表示（`:focus-visible`）を消さない**
- タップ領域は**最低 44px**
- 見出しは `h1 → h2 → h3` の順。**飛ばさない。1ページに h1 は1つ**
- ブラウザの文字拡大（200%）で崩れないこと
- すべての写真に `alt` を書く（`images.json` の `alt`）
- `prefers-reduced-motion: reduce` を尊重する（CSS に実装済み。無効化しない）
- 電話番号は必ず `tel:` リンクにする（`site.json` の `telHref` を使う）
- 画像には `width` / `height` 相当を持たせ、レイアウトのガタつき（CLS）を防ぐ

### 速度の目安

- TOP の初期転送量：**500KB 以下**
- Hero 画像単体：**150KB 以下**
- `/access/` は**特に軽量に**（現地で電波の弱い場所から開かれるため）
- Google Maps の iframe を**埋め込まない**（外部の地図アプリを開くリンクにする）

---

## 8. ファイル構成

```
src/
├── data/          ← 内容の更新はほぼここだけで足りる
│   ├── site.json      霊園の基本情報（住所・電話・墓参期間・追悼式）
│   ├── plans.json     費用（唯一の正本）
│   ├── faq.json       よくあるご質問
│   ├── trees.json     花木
│   ├── rules.json     霊園規約（重要事項＋全文）
│   └── images.json    写真の一元管理
├── images/            サイトで使う写真（npm run images が生成）
│   ├── work/          写真のアップロード先（公開ビルドには含まれない）
│   └── source/        元画像の保管庫（Git管理外）
├── styles/global.css  デザイントークンと全スタイル
├── components/    Header / Footer / Hero / Figure / FaqList / PlanCard / CtaBar / Breadcrumb
├── layouts/BaseLayout.astro   <head>・SEO・JSON-LD・スクリプト
└── pages/         / concept / plans / faq / access / about / rules / 404
scripts/
├── derive-images.mjs  作業用画像から派生画像を生成
├── image-recipes.json 切り出し方の設計図
├── check-todo.mjs     未確認プレースホルダの検査
└── check-exif.mjs     画像に EXIF（位置情報）が残っていないかの検査
public/
├── robots.txt
├── _redirects     旧URL（pgNN.html）からの転送 ※対応が判明したら追記
└── _headers       キャッシュとセキュリティヘッダー
```

---

## 8-2. 霊園規約を更新する

編集するのは **`src/data/rules.json` だけ**です。`/rules/` は「重要事項 → 規約全文」の二段構成です。

**規約全文（第1条〜第26条＋附則）は掲載済み**です。`fullText.articles` に1条ずつ、原文のまま入っています。

```json
{ "chapter": "第三章　樹木葬墓地", "no": "第１条", "title": "名称", "paragraphs": ["原文の第1項", "原文の第2項"] }
```

- `chapter` が前の条から変わったところに、章の見出しが自動で入ります
- `fullText.preface` は、全文の前に出す注記です（第15条が旧制度である旨など）

**全文を入れるときの絶対ルール**

- **原文をそのまま入れる。要約しない。条件を省略しない。言い回しを整えない。**
- 読みやすさのための調整は、**見出し・余白・改行など見た目だけ**に留める
- 原文が確認できない条文を、**推測で書き起こさない**
- `summary`（重要事項）は「規約の内容にもとづく要点」であり、条文そのものではありません。
  ページ上でもそう明記しています。この但し書きを消さないこと
- **概要だけを載せて全文を省略した状態で「規約を掲載した」と扱わないこと**

## 9. SEO のルール

- **「植樹葬」だけでなく、必ず「樹木葬」も自然に使う。**
  一般の人が検索するのは「樹木葬 岩手」「樹木葬 八戸」です。「植樹葬」は検索数がほとんどありません。
- キーワードの不自然な詰め込みは禁止。
- `title` / `description` はページごとに `BaseLayout` へ渡します。
- JSON-LD は **schema.org に実在する型だけ**を使い、**確認できない項目を入れない**。
  現在使っているのは `Cemetery` / `FAQPage` / `BreadcrumbList` の3つです。
  緯度経度・営業時間は未確認のため入れていません。
- `sitemap-index.xml` は自動生成されます。

---

## 10. 公開前の確認事項

```bash
npm run build     # ビルド + 未確認プレースホルダの検査
npm run preview   # 本番と同じ状態で確認
```

- [ ] `npm run build` が成功する（`{{TODO` が残っていない／画像に EXIF が残っていない）
- [ ] **スマートフォン幅（375px）で確認した**
- [ ] 電話番号がすべて `tel:` リンクになっていて、1タップで発信できる
- [ ] 新しく書いた事実に、確認できていない推測が混ざっていない
- [ ] 写真を追加したなら `alt` を書いた
- [ ] 見出しの順番が飛んでいない
- [ ] 文字を200%に拡大しても読める
- [ ] 料金を変えたなら、TOP と `/plans/` の両方に反映されている

---

## 10-2. 技術方針（確定）

**Astro + GitHub + Cloudflare Pages で完成・公開します。**

- Wix Studio への移行は**行いません**（2026年8月に方針確定）
- ホスティングは Cloudflare Pages。`public/_redirects` と `public/_headers` が有効になります
- 本番ドメインは `shokuju-reien.jp`（接続は制作完了後）
- 制作中は `*.pages.dev` のプレビューURLで確認します

## 11. やらないこと

- AI生成画像
- Hero動画
- スクロールジャック / Lenis
- 自動再生の音
- チャットボット
- React・Next.js の導入（このサイトに動的な要素はありません）
- 複雑なCMSの導入
- Wix 等の Website Builder への移行（Astro で完成させる方針）
- 旧サイトの `pgNN.html` というURL構造の踏襲
- 料金や制度の推測
- 「継承者不要」という単純な断定
- 「森の会員」など正式でないブランド表現
- 旧サイト文章の無批判なコピペ
- 英語中心のナビゲーション（見出し・メニューは日本語）

---

## 12. 未確認事項

現在の未確認リストは **`docs/TODO.md`** にまとまっています。
新しく分かったことがあれば、そこにチェックを入れてから本文を直してください。
